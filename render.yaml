services:
  - type: web
    name: two-minute-tycoon
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
    disk:
      name: data-volume
      mountPath: /.data
      sizeGB: 1


